// This is an example file.
//
// 1. Copy this file to a new file named `firebase-config.js`
// 2. Replace the placeholder values with your actual Firebase project credentials.
// 3. The `firebase-config.js` file is already in .gitignore, so it will not be committed.

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  databaseURL: "YOUR_DATABASE_URL",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};